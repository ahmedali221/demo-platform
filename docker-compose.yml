services:
  frontend:
    build:
      context: .
      args:
        BASEURL: ${BASEURL:-http://localhost/api}
    container_name: ops_frontend
    restart: unless-stopped
    ports:
      - "3000:80"
